# agentic-rag
Agentic RAG system built with FastAPI that ingests documents, splits them into chunks, generates embeddings, and stores them in a vector database. On query, it retrieves relevant context and uses an LLM to produce accurate, grounded responses with reduced hallucination and improved reliability.
